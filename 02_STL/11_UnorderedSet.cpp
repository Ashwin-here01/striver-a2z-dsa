#include <bits/stdc++.h>
using namespace std;

// Unique
// ANY Random order

int main() {
    unordered_set<int> st;
    // Lower_bound and Upper_bound doesn't work... Rest all functions are same
    // Time complexity : O(1) [except some case when collision happen O(n)]

    return 0;
}